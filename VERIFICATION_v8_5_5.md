# Проверка v8.5.5 — полный проход цепочки и кнопок

## Что исправлено

1. Всплывающее уведомление больше не перекрывает кнопку «Проверить архитектуру». Теперь у уведомления отключены клики мыши/тача (`pointer-events: none`).
2. Версия в интерфейсе обновлена: `v8.5.5-button-smoke-fixed`.

## Проверки ядра

```text
python run_tests.py
68/68 passed
```

```text
pytest -q -rs
19 passed, 2 skipped
```

Два пропуска — стандартные Playwright-тесты, которые ищут встроенный браузер Playwright. Дополнительно выполнен отдельный браузерный прогон через системный Chromium `/usr/bin/chromium`.

## Проверка матриц

```text
python verify_action_grammar_matrix.py
checked=2880 failures=0 colors={'green': 420, 'yellow': 2400, 'red': 60}
```

```text
python verify_full_stack_coverage.py
channels=21 single=21 pairwise=441 issues=0
```

## Живой HTTP smoke

```text
GET /health -> {"ok": true}
POST /api/analyze -> {"ok": true, "id": "..."}
GET /run/<id> -> 200
GET /run/<id>.md -> markdown report generated
```

В markdown-отчёте есть раздел с объяснением выбора технологий и решений: почему выбрано именно это, почему не другой вариант, какие условия обязательны.

## Браузерный прогон кнопок через Chromium

Проверено:

- открытие главной страницы;
- переключение простой/экспертный режим;
- wizard «собрать черновик из вариантов»;
- выбор всех 5 шагов мастера;
- автосборка цепочки;
- добавление всех 10 усложнений;
- переход в экспертный режим;
- безопасные настройки всем шагам;
- добавление всех 8 типов шагов из палитры;
- добавление 6 типов участников;
- перемещение шага вверх/вниз;
- дублирование шага;
- вставка шага до/после;
- безопасные настройки конкретного шага;
- ручное переопределение стека на RabbitMQ;
- возврат автоподбора стека;
- запуск кнопкой «Проверить архитектуру»;
- проверка payload: нет ссылок на отсутствующие системы, нет self-dependency, нет depends_on на несуществующий шаг;
- ошибок в консоли браузера нет.

Результат:

```text
SUMMARY 29 ok 0 fail
```

## Вывод

Цепочка от UI до формирования отчёта работает. Найденный дефект с перекрытием кнопки всплывающим уведомлением исправлен. Внутренние зависимости и ссылки в payload после сложных действий в конструкторе остаются валидными.
