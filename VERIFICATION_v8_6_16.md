# Проверка v8.6.16 — глубокая логика связей и процесса

## Что исправлено

1. Исправлена потеря семантики связи при добавлении шага: теперь сохраняются `interaction_action`, `interaction_timing`, `interaction_result`, `dependency_basis`, `primary_channel`.
2. Валидатор перед подбором стека стал проверять не только пустые поля и самозависимости, а смысловую корректность процесса:
   - передача данных в БД вместо сохранения;
   - async-запрос партнёру без обратного статуса;
   - перепутанное направление позднего статуса;
   - рассылка многим системам без брокера/журнала событий;
   - файл, направленный в обычную БД;
   - аналитическая связь не в аналитический контур;
   - старт от пассивного участника;
   - отсутствующий владелец процесса;
   - отсутствующие участники в связях;
   - некорректные зависимости.
3. Автоправки теперь умеют не только менять одно поле, но и:
   - менять набор полей шага;
   - пересобирать название связи;
   - добавлять недостающий шаг, например входящий поздний статус от партнёра.
4. Исправлена логика выбора канала, когда источником является БД, а получателем аналитика: БД больше не подменяет основной канал связи.

## Проверки

- `python run_tests.py` → 68/68 passed
- `pytest -q -rs` → 29 passed
- `python verify_action_grammar_matrix.py` → checked=2880 failures=0
- `python verify_full_stack_coverage.py` → channels=55 single=55 pairwise=3025 issues=0
- `python verify_semantic_question_stack_coverage.py` → semantic_options=55 channels=55 issues=0
- `python verify_branch_question_stack_flow.py` → branch_questions=60 channels=55 issues=0
- `python verify_complex_e2e_v860.py` → 5 сложных кейсов, failed=0, покрыто 55/55 технологий
- `python verify_ui_real_user_path_v860.py` → 20 ok, 0 fail
- `python verify_single_process_map_v8611.py` → single_process_map=ok
- `python verify_schema_validation_v8615.py` → 5 ok, 0 fail
- `python verify_deep_schema_validation_v8616.py` → 7 ok, 0 fail

## Итог

Механизм проверки схемы стал значительно умнее: он проверяет процесс до подбора стека, показывает пользователю проблемные связи, предлагает конкретные улучшения и умеет применять безопасные автоправки. При этом существующее ядро анализа, матрица стека, отчёты и основной пользовательский путь не сломаны.
